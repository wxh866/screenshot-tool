# Normalize line endings to LF in the repo (cross-platform consistency)
* text=auto eol=lf

# Binary / build artifacts (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.exe binary
*.dll binary
